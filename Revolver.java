/*
public class Revolver extends Weapon{

    public Revolver(double weight, String name, int price, int damage) {
        super(weight, name, price, damage);
        setName("Revolver");
        setPrice(10);
        setWeight(1.2);
        setDamage(10);
    }

    @Override
    public void doCommand(String command, Player player) {
        if (command.equals("Shoot")){
            System.out.println("You shot the gun!");
        }
        else{
            System.out.println("You didn't shoot the gun");
        }
    }
}
*/
